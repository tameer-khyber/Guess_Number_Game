import 'package:flutter/material.dart';

/// Neon Dreams theme colors - Vibrant neon colors with glowing effects
class NeonDreamsColors {
  // Primary Colors
  static const Color primary = Color(0xFFFF00FF); // Neon Magenta
  static const Color secondary = Color(0xFF00FFFF); // Neon Cyan
  static const Color accent = Color(0xFFFFFF00); // Neon Yellow
  
  // Background Colors
  static const List<Color> backgroundGradient = [
    Color(0xFF0A0A1E), // Deep dark purple
    Color(0xFF1A0A2E), // Dark purple
  ];
  
  // Text Colors
  static const Color text = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0FF);
  
  // Glass Effect
  static const Color glassBackground = Color(0xFF1A0A3E);
  static const Color glassBorder = Color(0xFFFF00FF);
  
  // Status Colors
  static const Color success = Color(0xFF00FF88); // Neon Green
  static const Color error = Color(0xFFFF0055); // Neon Red
  static const Color warning = Color(0xFFFFCC00); // Neon Orange-Yellow
  static const Color info = Color(0xFF00CCFF); // Neon Blue
  
  // Card Colors
  static const Color cardBackground = Color(0xFF1F0F3F);
  static const Color cardBorder = Color(0xFFFF00FF);
}
